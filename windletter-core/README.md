# windletter-core

Shared foundations for the library:

- protocol-independent constants
- error model / status model
- common encoding and utility helpers

Current baseline:

- shared `ErrorCode` enum in `com.windletter.core.error`
